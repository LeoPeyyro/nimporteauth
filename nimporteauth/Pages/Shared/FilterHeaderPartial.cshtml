@page
@model nimporteauth.Pages.Shared.FilterHeaderPartialModel
@{
    // var sortOrder = HttpContext.Request.Query["sortOrder"].FirstOrDefault();
    // var isReverse = HttpContext.Request.Query["reverse"].Any(x => x.Equals("true", StringComparison.CurrentCultureIgnoreCase));
}
@* @if (Model != null)
{

    <a asp-page="./Index"
       asp-route-sortOrder="@(Model.SortProperty)"
       asp-route-reverse="@(Model.SortProperty == sortOrder && !isReverse ? true : false)">
        @Model.Name
    </a>

} *@